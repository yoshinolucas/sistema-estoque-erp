@model Produto
@{
    ViewData["Title"] = "Cadastrar Produto";
}

<div class="text-center">

    <h1 class="display-4 text-light">Cadastrar Produto</h1>
    
</div>

<div class="container">
    <form class="py-4 mt-4 border d-grid gap-2 col-6 mx-auto text-light" asp-controller="Produto" asp-action="CriarProduto" method="post">
        <div class="form-group">
            <label for="nome">Nome</label>
            <input asp-for="Nome" class="form-control" placeholder="Digite o nome do produto"/>
            @Html.ValidationMessageFor(x => x.Nome)
        </div>
        <div class="form-group">
            <label for="preco">Preço</label>
            <input type="number" value="0.00" min="0.00" step="0.00" asp-for="Preco" class="form-control" placeholder="Digite o valor do produto"/>
            @Html.ValidationMessageFor(x => x.Preco)
        </div>
        <div class="form-group">
            <label for="estoque">Estoque</label>
            <input type="number" value="0"asp-for="Estoque" class="form-control" placeholder="Digite o estoque inicial do produto"/>
            @Html.ValidationMessageFor(x => x.Estoque)
        </div>
        <div class="form-group">
            <label for="categoria" class="form-label">Categoria</label>
            <select required asp-for="Id_Categoria" asp-items="ViewBag.opcoesCategoria" class="form-control">       
                <option selected>Selecionar</option>
            </select>           
            @Html.ValidationMessageFor(x => x.Id_Categoria)

        </div>

        <div class="d-grid gap-2 col-6 mx-auto">
            <button type="submit" class="btn btn-primary btn-block">Salvar</button>
            <a class="btn btn-outline-warning btn-block" asp-controller="Produto" asp-action="Index">Voltar</a>
        </div>
        
    </form>
</div>
